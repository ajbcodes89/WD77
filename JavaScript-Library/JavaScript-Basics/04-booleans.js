//when you need a daya type that can only have one of two possible values, BOOLEAN IS THE WebAssembly
// yes/no
// on/off
// true/false

let isLoggedIn = true;
let isAuthenticated = true;
let hasLoggedInToday = false;
let hasToken = false;

// equality vs strict equality?????
// equality converts operands if they are not the same type
// strict equality or "identity" returns true if the operands are strictly equal meaning they do not require type conversion to be equal!

console.log(2 > 1); //true
console.log(3 < 2); //false

var test = 2>=3;
console.log(test);
console.log("two is greater than one?" + (2>1));
console.log(3>=1);

console.log("Password12!" === "Password12!!");
console.log(1 !== 2);
console.log(10 !== 10);   

console.log("&& :", 2===2 && 1===1 ); //true because?
console.log("|| :", 2===2 || 2===1 ); //true because?
console.log("!=", 2 != 1); //true because?

// AND &&
// OR ||
// NOT !

// == Equality
// === Strict Equality
// != Inequality
// !== Strict Inequality
// > Greater Than
// >= Greater than or Equal
// < Less Than
// <= Less than or Equal

