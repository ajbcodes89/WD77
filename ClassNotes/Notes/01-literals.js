//whet is a literal bruv
//a literal is a representation of a value in js
//it is a fixed value, not a varibale
// literally provided in the code

// a variable is not a literal but the information assigned to a variable is a literal

/*
(keyword)
let    placeholder = 'text'
        (variable)    (value/literal)
*/

// STRING LITERAL
let city = 'indianapolis';
// NUMERIC LITERAL
let num = 123;
// BOOLEAN LITERAL
let isOn = true;
// OBJECT LITERAL
let movie = {
    title: 'Elf',
    rating: 'PG',
}
// ARRAY LIERAL
let daysOfWeek = ['Sunday', 'Monday', 'Tuesday'];
